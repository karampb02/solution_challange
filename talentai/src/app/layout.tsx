import type { Metadata } from "next";
import { Geist, Geist_Mono } from "next/font/google";
import "./globals.css";

const geistSans = Geist({
  variable: "--font-geist-sans",
  subsets: ["latin"],
});

const geistMono = Geist_Mono({
  variable: "--font-geist-mono",
  subsets: ["latin"],
});

export const metadata: Metadata = {
  title: "TalentAI — Zero-Friction Talent Intelligence",
  description:
    "AI-driven platform that uses Gemini and semantic search to match technical talent based on deep project context, intent, and culture. Zero manual effort required.",
  keywords: [
    "AI hiring",
    "talent matching",
    "Gemini AI",
    "semantic search",
    "technical recruiting",
  ],
};

export default function RootLayout({
  children,
}: Readonly<{
  children: React.ReactNode;
}>) {
  return (
    <html
      lang="en"
      className={`${geistSans.variable} ${geistMono.variable} h-full antialiased`}
    >
      <body className="min-h-full flex flex-col">{children}</body>
    </html>
  );
}
